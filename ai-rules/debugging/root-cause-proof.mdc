---
description: Root cause must be evidenced end-to-end, not assumed
globs: "**/*.{ts,tsx,js,jsx,html,css,py,go,rs,java,kt,cs,php,rb,sql,vue,svelte}"
alwaysApply: false
---

# Root cause proof

Root cause must explain the symptom end-to-end on the traced path.

Required: cite evidence (log line, stack frame, code branch, state value).

If only a hypothesis exists, label it and run disproof first.
