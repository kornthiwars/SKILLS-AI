# OS
.DS_Store
Thumbs.db

# Secrets & credentials (never commit)
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# Editor / local IDE
.idea/
*.swp
*~

# Cursor local setup (junctions/symlinks from scripts — edit ai-skills/ ai-rules/ instead)
.cursor/*
!.cursor/README.md
!.cursor/hooks.json

# Machine-local skill overrides
ai-skills/.local/

# Vault — user-local content only (.gitkeep tracks the folder)
vault/**
!vault/.gitkeep
