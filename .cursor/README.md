# .cursor (local)

This folder is created by [scripts/setup-macos-linux.sh](../scripts/setup-macos-linux.sh) or [setup-windows.ps1](../scripts/setup-windows.ps1).

| Link | Canonical source |
|------|------------------|
| `skills/` | `../ai-skills/` |
| `rules/` | `../ai-rules/` |
| `vault/` | `../vault/` |

Edit **`ai-skills/`**, **`ai-rules/`**, and **`vault/`** in the repo root — not copies under `.cursor/` unless you intentionally override.

Optional: `hooks.json` in this folder (not overwritten by setup).
